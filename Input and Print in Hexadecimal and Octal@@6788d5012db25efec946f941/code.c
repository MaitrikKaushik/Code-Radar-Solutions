#include <stdio.h>

#include <stdio.h>

int main() {
    int num;
    // Taking integer input
    scanf("%d", &num);
    
    // Printing hexadecimal (uppercase) and octal representations
    printf("Hexadecimal: %X\n", num);
    printf("Octal: %o\n", num);

    return 0;
}
